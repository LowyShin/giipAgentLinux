################################################################################
# ⚠️⚠️⚠️ WARNING: THIS IS A SAMPLE/TEMPLATE FILE ⚠️⚠️⚠️
#
# DO NOT use this file to diagnose production issues!
# DO NOT read values from this file in git repository!
# 
# This file in git repository is ONLY A TEMPLATE for new installations.
# The ACTUAL configuration file is deployed on each server at:
#   Linux: ~/giipAgent/giipAgent.cnf
#   
# To check REAL production values, SSH to the actual server:
#   ssh user@server "cat ~/giipAgent/giipAgent.cnf"
#
################################################################################

# You can find secret key and lssn from giip user portal (web UI)
sk="<your secret key>"

# If you have lssn, put below.
lssn="0"

# Agent execution interval (seconds)
# 60 = Check queue every 60 seconds
giipagentdelay="60"

# Gateway Mode (NEW in v1.80)
# Set to "1" to enable Gateway mode for managing remote servers
# Set to "0" for normal local agent mode
# 
# When enabled:
#   - Automatically fetches remote server list from GIIP Web UI
#   - No manual CSV file editing needed
#   - All configuration done via Web UI (lsvrdetail page)
#   - Auto-installs sshpass if needed
#   - Supports both SSH key and password authentication
gateway_mode="0"

# Gateway server list cache file (auto-generated from API)
gateway_serverlist="./giipAgentGateway_servers.csv"

# Gateway database query list cache file (auto-generated from API)
# Supports: MySQL, MariaDB, MSSQL, PostgreSQL, Oracle, MongoDB, Redis
gateway_db_querylist="./giipAgentGateway_db_queries.csv"

# Auto-sync interval (seconds)
# How often to refresh server list from Web UI
# Default: 300 = 5 minutes
gateway_sync_interval="300"

# API v2 (Recommended) - PowerShell-based, faster and more stable
apiaddrv2="https://giipfaw.azurewebsites.net/api/giipApiSk2"
apiaddrcode="YOUR_AZURE_FUNCTION_KEY_HERE"

# API v1 (Legacy) - ASP Classic-based
apiaddr="https://giipasp.azurewebsites.net"

